import { Typography } from '@mui/material';
import React from 'react';

const Navbar = () => {
  return (
    <Typography variant="h4" py={2} alignSelf="start">
      Inventory Management System
    </Typography>
  );
};

export default Navbar;
